-- The live deployment initially received the transfer functions while this feature was
-- being built. Their final whole-number validation is included in the preceding
-- add_pos_stock_transfers migration so new environments receive the final definitions.
select 1;
