create table public.pos_used_device_acquisitions (
  id bigint generated by default as identity primary key,
  acquisition_code text not null unique,
  store_id bigint not null references public.store_locations(id) on delete restrict,
  shift_id text not null references public.pos_store_shifts(shift_code) on delete restrict,
  seller_name text not null,
  seller_phone text not null,
  seller_email text not null default '',
  seller_address text not null,
  seller_id_type text not null,
  seller_id_reference text not null,
  seller_age_confirmed boolean not null,
  seller_is_owner boolean not null,
  owner_name text not null default '',
  owner_address text not null default '',
  acquisition_statement text not null,
  ownership_declaration boolean not null,
  payout_method text not null check (payout_method in ('Cash', 'Bank Transfer')),
  payout_amount numeric(12,2) not null check (payout_amount > 0),
  declaration_text text not null,
  acquired_by text not null,
  acquired_at timestamptz not null default now(),
  created_at timestamptz not null default now()
);

create index pos_used_device_acquisitions_store_date_idx
on public.pos_used_device_acquisitions (store_id, acquired_at desc);

create index pos_used_device_acquisitions_shift_idx
on public.pos_used_device_acquisitions (shift_id)
where shift_id is not null;

create or replace function public.validate_pos_used_device_acquisition_shift()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
begin
  if not exists (
    select 1
    from public.pos_store_shifts shift_record
    where shift_record.shift_code = new.shift_id
      and shift_record.store_id = new.store_id
      and shift_record.status = 'open'
      and shift_record.closed_at is null
  ) then
    raise exception 'An open shift for this store is required before buying a device';
  end if;
  return new;
end;
$$;

create trigger pos_used_device_acquisitions_validate_shift
before insert or update of shift_id, store_id on public.pos_used_device_acquisitions
for each row execute function public.validate_pos_used_device_acquisition_shift();

create table public.pos_used_devices (
  id bigint generated by default as identity primary key,
  device_code text not null unique,
  acquisition_id bigint not null references public.pos_used_device_acquisitions(id) on delete restrict,
  store_id bigint not null references public.store_locations(id) on delete restrict,
  category text not null check (category in ('Phone', 'Tablet', 'Laptop', 'Watch', 'Game Console', 'Other')),
  brand text not null,
  model text not null,
  variant text not null default '',
  color text not null default '',
  storage text not null default '',
  imei text not null default '',
  normalized_imei text not null default '',
  serial_number text not null default '',
  condition_grade text not null check (condition_grade in ('As New', 'Good', 'Fair', 'Poor', 'Faulty')),
  battery_health integer check (battery_health is null or battery_health between 0 and 100),
  inspection jsonb not null default '{}'::jsonb,
  clean_check_status text not null check (clean_check_status in ('Clean', 'Blocked', 'Pending', 'Not Applicable')),
  clean_check_reference text not null default '',
  clean_checked_at timestamptz,
  activation_lock_removed boolean not null default false,
  data_erased_confirmed boolean not null default false,
  purchase_cost numeric(12,2) not null check (purchase_cost > 0),
  sale_price numeric(12,2) not null check (sale_price > 0),
  status text not null check (status in ('inspection', 'ready_for_sale', 'sold', 'returned_to_seller', 'disposed')),
  notes text not null default '',
  photo_urls jsonb not null default '[]'::jsonb,
  acquired_by text not null,
  acquired_at timestamptz not null default now(),
  updated_by text not null,
  ready_at timestamptz,
  sold_order_id bigint references public.pos_sales_orders(id) on delete restrict,
  sold_order_line_id bigint references public.pos_sales_order_lines(id) on delete restrict,
  sold_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint pos_used_devices_identifier_required check (normalized_imei <> '' or serial_number <> '')
);

create unique index pos_used_devices_imei_unique
on public.pos_used_devices (normalized_imei)
where normalized_imei <> '';

create unique index pos_used_devices_serial_unique
on public.pos_used_devices (lower(serial_number))
where serial_number <> '';

create index pos_used_devices_store_status_idx
on public.pos_used_devices (store_id, status, updated_at desc);

create index pos_used_devices_acquisition_idx
on public.pos_used_devices (acquisition_id);

create table public.pos_used_device_transactions (
  id bigint generated by default as identity primary key,
  transaction_code text not null unique,
  device_id bigint not null references public.pos_used_devices(id) on delete restrict,
  store_id bigint not null references public.store_locations(id) on delete restrict,
  transaction_type text not null check (transaction_type in (
    'acquisition', 'status_change', 'price_change', 'sale', 'refund_return',
    'returned_to_seller', 'disposal'
  )),
  from_status text,
  to_status text,
  amount numeric(12,2) not null default 0 check (amount >= 0),
  payment_method text not null default '',
  related_sales_order_id bigint references public.pos_sales_orders(id) on delete restrict,
  related_sales_order_line_id bigint references public.pos_sales_order_lines(id) on delete restrict,
  staff_name text not null,
  counterparty_name text not null default '',
  counterparty_phone text not null default '',
  notes text not null default '',
  transaction_payload jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now()
);

create index pos_used_device_transactions_store_date_idx
on public.pos_used_device_transactions (store_id, created_at desc);

create index pos_used_device_transactions_device_idx
on public.pos_used_device_transactions (device_id, created_at desc);

alter table public.pos_sales_order_lines
  drop constraint if exists pos_sales_order_lines_line_type_check;

alter table public.pos_sales_order_lines
  add constraint pos_sales_order_lines_line_type_check
  check (line_type in ('product', 'repair', 'special', 'used_device'));

alter table public.pos_sales_order_lines
  add column used_device_id bigint references public.pos_used_devices(id) on delete restrict;

create index pos_sales_order_lines_used_device_idx
on public.pos_sales_order_lines (used_device_id)
where used_device_id is not null;

create index pos_used_devices_sold_order_idx
on public.pos_used_devices (sold_order_id)
where sold_order_id is not null;

create index pos_used_devices_sold_order_line_idx
on public.pos_used_devices (sold_order_line_id)
where sold_order_line_id is not null;

create index pos_used_device_transactions_sales_order_idx
on public.pos_used_device_transactions (related_sales_order_id)
where related_sales_order_id is not null;

create index pos_used_device_transactions_sales_order_line_idx
on public.pos_used_device_transactions (related_sales_order_line_id)
where related_sales_order_line_id is not null;

drop trigger if exists pos_used_devices_set_updated_at on public.pos_used_devices;
create trigger pos_used_devices_set_updated_at
before update on public.pos_used_devices
for each row execute function public.set_updated_at();

alter table public.pos_used_device_acquisitions enable row level security;
alter table public.pos_used_devices enable row level security;
alter table public.pos_used_device_transactions enable row level security;

revoke all on public.pos_used_device_acquisitions from public, anon, authenticated;
revoke all on public.pos_used_devices from public, anon, authenticated;
revoke all on public.pos_used_device_transactions from public, anon, authenticated;

grant select, insert on public.pos_used_device_acquisitions to service_role;
grant select, insert, update on public.pos_used_devices to service_role;
grant select, insert on public.pos_used_device_transactions to service_role;

create or replace function public.pos_used_device_payload(device_row public.pos_used_devices)
returns jsonb
language sql
stable
set search_path = ''
as $$
  select jsonb_build_object(
    'id', device_row.device_code,
    'device_code', device_row.device_code,
    'store_code', store_location.store_code,
    'store_name', store_location.store_name,
    'category', device_row.category,
    'brand', device_row.brand,
    'model', device_row.model,
    'variant', device_row.variant,
    'color', device_row.color,
    'storage', device_row.storage,
    'imei', device_row.imei,
    'serial_number', device_row.serial_number,
    'condition_grade', device_row.condition_grade,
    'battery_health', device_row.battery_health,
    'inspection', device_row.inspection,
    'clean_check_status', device_row.clean_check_status,
    'clean_check_reference', device_row.clean_check_reference,
    'clean_checked_at', device_row.clean_checked_at,
    'activation_lock_removed', device_row.activation_lock_removed,
    'data_erased_confirmed', device_row.data_erased_confirmed,
    'purchase_cost', device_row.purchase_cost,
    'sale_price', device_row.sale_price,
    'status', device_row.status,
    'notes', device_row.notes,
    'photo_urls', device_row.photo_urls,
    'acquired_by', device_row.acquired_by,
    'acquired_at', device_row.acquired_at,
    'updated_by', device_row.updated_by,
    'ready_at', device_row.ready_at,
    'sold_at', device_row.sold_at,
    'sold_order_id', sales_order.order_code,
    'sold_invoice_number', sales_order.invoice_number,
    'seller', jsonb_build_object(
      'name', acquisition.seller_name,
      'phone', acquisition.seller_phone,
      'email', acquisition.seller_email,
      'address', acquisition.seller_address,
      'id_type', acquisition.seller_id_type,
      'id_reference', acquisition.seller_id_reference,
      'age_confirmed', acquisition.seller_age_confirmed,
      'is_owner', acquisition.seller_is_owner,
      'owner_name', acquisition.owner_name,
      'owner_address', acquisition.owner_address,
      'acquisition_statement', acquisition.acquisition_statement,
      'ownership_declaration', acquisition.ownership_declaration
    ),
    'acquisition', jsonb_build_object(
      'id', acquisition.acquisition_code,
      'shift_id', acquisition.shift_id,
      'payout_method', acquisition.payout_method,
      'payout_amount', acquisition.payout_amount,
      'acquired_by', acquisition.acquired_by,
      'acquired_at', acquisition.acquired_at
    ),
    'created_at', device_row.created_at,
    'updated_at', device_row.updated_at
  )
  from public.pos_used_device_acquisitions acquisition
  join public.store_locations store_location on store_location.id = device_row.store_id
  left join public.pos_sales_orders sales_order on sales_order.id = device_row.sold_order_id
  where acquisition.id = device_row.acquisition_id;
$$;

create or replace function public.search_pos_used_devices(
  session_token text,
  target_store_code text,
  search_query text default '',
  target_status text default '',
  result_limit integer default 200
)
returns jsonb
language plpgsql
security definer
set search_path = ''
as $$
declare
  selected_store public.store_locations%rowtype;
  query_value text := trim(coalesce(search_query, ''));
  status_value text := trim(coalesce(target_status, ''));
  safe_limit integer := least(greatest(coalesce(result_limit, 200), 1), 500);
  devices_payload jsonb;
begin
  if not public.is_valid_staff_session(session_token) then raise exception 'Invalid session'; end if;

  select * into selected_store
  from public.store_locations store_location
  where store_location.active = true
    and store_location.store_code = coalesce(trim(target_store_code), '')
    and store_location.store_code <> 'warehouse';
  if not found then raise exception 'Store not found'; end if;

  if status_value <> '' and status_value not in ('inspection', 'ready_for_sale', 'sold', 'returned_to_seller', 'disposed') then
    raise exception 'Invalid device status';
  end if;

  select coalesce(
    jsonb_agg(public.pos_used_device_payload(device) order by
      case device.status when 'ready_for_sale' then 1 when 'inspection' then 2 when 'sold' then 3 else 4 end,
      device.updated_at desc
    ),
    '[]'::jsonb
  ) into devices_payload
  from (
    select used_device.*
    from public.pos_used_devices used_device
    join public.pos_used_device_acquisitions acquisition on acquisition.id = used_device.acquisition_id
    where used_device.store_id = selected_store.id
      and (status_value = '' or used_device.status = status_value)
      and (
        query_value = ''
        or used_device.device_code ilike '%' || query_value || '%'
        or used_device.brand ilike '%' || query_value || '%'
        or used_device.model ilike '%' || query_value || '%'
        or used_device.variant ilike '%' || query_value || '%'
        or used_device.color ilike '%' || query_value || '%'
        or used_device.storage ilike '%' || query_value || '%'
        or used_device.imei ilike '%' || query_value || '%'
        or used_device.serial_number ilike '%' || query_value || '%'
        or acquisition.seller_name ilike '%' || query_value || '%'
        or acquisition.seller_phone ilike '%' || query_value || '%'
      )
    order by used_device.updated_at desc
    limit safe_limit
  ) device;

  return jsonb_build_object(
    'ok', true,
    'store_code', selected_store.store_code,
    'store_name', selected_store.store_name,
    'devices', devices_payload,
    'summary', (
      select jsonb_build_object(
        'inspection_count', count(*) filter (where status = 'inspection'),
        'ready_count', count(*) filter (where status = 'ready_for_sale'),
        'sold_count', count(*) filter (where status = 'sold'),
        'stock_cost', coalesce(sum(purchase_cost) filter (where status in ('inspection', 'ready_for_sale')), 0),
        'stock_retail', coalesce(sum(sale_price) filter (where status in ('inspection', 'ready_for_sale')), 0),
        'realized_margin', coalesce(sum(sale_transaction.amount - used_device.purchase_cost) filter (where used_device.status = 'sold'), 0)
      )
      from public.pos_used_devices used_device
      left join lateral (
        select ledger.amount
        from public.pos_used_device_transactions ledger
        where ledger.device_id = used_device.id and ledger.transaction_type = 'sale'
        order by ledger.created_at desc
        limit 1
      ) sale_transaction on true
      where used_device.store_id = selected_store.id
    )
  );
end;
$$;

create or replace function public.get_pos_used_device_transactions(
  session_token text,
  target_store_code text,
  search_query text default '',
  result_limit integer default 200
)
returns jsonb
language plpgsql
security definer
set search_path = ''
as $$
declare
  selected_store public.store_locations%rowtype;
  query_value text := trim(coalesce(search_query, ''));
  safe_limit integer := least(greatest(coalesce(result_limit, 200), 1), 500);
  rows_payload jsonb;
begin
  if not public.is_valid_staff_session(session_token) then raise exception 'Invalid session'; end if;
  select * into selected_store
  from public.store_locations store_location
  where store_location.active = true
    and store_location.store_code = coalesce(trim(target_store_code), '')
    and store_location.store_code <> 'warehouse';
  if not found then raise exception 'Store not found'; end if;

  select coalesce(jsonb_agg(to_jsonb(row_data) order by row_data.created_at desc), '[]'::jsonb)
  into rows_payload
  from (
    select
      ledger.transaction_code as id,
      ledger.transaction_type as type,
      device.device_code,
      device.brand,
      device.model,
      device.imei,
      device.serial_number,
      ledger.from_status,
      ledger.to_status,
      ledger.amount,
      ledger.payment_method,
      ledger.staff_name,
      ledger.counterparty_name,
      ledger.counterparty_phone,
      ledger.notes,
      sales_order.order_code as order_id,
      sales_order.invoice_number,
      ledger.created_at
    from public.pos_used_device_transactions ledger
    join public.pos_used_devices device on device.id = ledger.device_id
    left join public.pos_sales_orders sales_order on sales_order.id = ledger.related_sales_order_id
    where ledger.store_id = selected_store.id
      and (
        query_value = ''
        or device.device_code ilike '%' || query_value || '%'
        or device.brand ilike '%' || query_value || '%'
        or device.model ilike '%' || query_value || '%'
        or device.imei ilike '%' || query_value || '%'
        or device.serial_number ilike '%' || query_value || '%'
        or ledger.counterparty_name ilike '%' || query_value || '%'
        or ledger.counterparty_phone ilike '%' || query_value || '%'
        or sales_order.order_code ilike '%' || query_value || '%'
        or sales_order.invoice_number::text ilike '%' || query_value || '%'
      )
    order by ledger.created_at desc
    limit safe_limit
  ) row_data;

  return jsonb_build_object('ok', true, 'transactions', rows_payload);
end;
$$;

create or replace function public.create_pos_used_device_acquisition(session_token text, payload jsonb)
returns jsonb
language plpgsql
security definer
set search_path = ''
as $$
declare
  selected_store public.store_locations%rowtype;
  selected_staff public.staff_directory%rowtype;
  acquisition_row public.pos_used_device_acquisitions%rowtype;
  device_row public.pos_used_devices%rowtype;
  acquisition_code_value text := 'BUY-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 12));
  device_code_value text := 'USED-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 12));
  seller_name_value text := trim(coalesce(payload->>'seller_name', ''));
  seller_phone_value text := trim(coalesce(payload->>'seller_phone', ''));
  seller_address_value text := trim(coalesce(payload->>'seller_address', ''));
  seller_is_owner_value boolean := lower(coalesce(payload->>'seller_is_owner', 'true')) = 'true';
  normalized_imei_value text := regexp_replace(coalesce(payload->>'imei', ''), '[^0-9]', '', 'g');
  serial_value text := trim(coalesce(payload->>'serial_number', ''));
  status_value text := coalesce(nullif(trim(payload->>'status'), ''), 'inspection');
  clean_status_value text := coalesce(nullif(trim(payload->>'clean_check_status'), ''), 'Pending');
  purchase_cost_value numeric(12,2) := round(coalesce(nullif(payload->>'purchase_cost', '')::numeric, 0), 2);
  sale_price_value numeric(12,2) := round(coalesce(nullif(payload->>'sale_price', '')::numeric, 0), 2);
  inspection_value jsonb := coalesce(payload->'inspection', '{}'::jsonb);
  declaration_value text := 'The seller declares they have the legal right to sell this device, the device is not financed, leased, lost, stolen, or network blocked, and the recorded information is correct.';
begin
  if not public.is_valid_staff_session(session_token) then raise exception 'Invalid session'; end if;
  if jsonb_typeof(payload) <> 'object' then raise exception 'Payload must be an object'; end if;

  select * into selected_store
  from public.store_locations store_location
  where store_location.active = true
    and store_location.store_code = coalesce(trim(payload->>'store_code'), '')
    and store_location.store_code <> 'warehouse';
  if not found then raise exception 'Store not found'; end if;

  select * into selected_staff
  from public.staff_directory staff
  where staff.active = true and lower(staff.display_name) = lower(coalesce(trim(payload->>'staff_name'), ''))
  limit 1;
  if not found then raise exception 'Staff member not found'; end if;

  if seller_name_value = '' then raise exception 'Seller name is required'; end if;
  if seller_phone_value = '' then raise exception 'Seller phone is required'; end if;
  if seller_address_value = '' then raise exception 'Seller address is required'; end if;
  if trim(coalesce(payload->>'seller_id_type', '')) = '' or trim(coalesce(payload->>'seller_id_reference', '')) = '' then
    raise exception 'Seller ID type and reference are required';
  end if;
  if lower(coalesce(payload->>'seller_age_confirmed', 'false')) <> 'true' then raise exception 'Seller age must be confirmed'; end if;
  if lower(coalesce(payload->>'ownership_declaration', 'false')) <> 'true' then raise exception 'Ownership declaration is required'; end if;
  if trim(coalesce(payload->>'acquisition_statement', '')) = '' then raise exception 'How the seller obtained the device is required'; end if;
  if not seller_is_owner_value and (
    trim(coalesce(payload->>'owner_name', '')) = '' or trim(coalesce(payload->>'owner_address', '')) = ''
  ) then raise exception 'Owner name and address are required when the seller is not the owner'; end if;

  if trim(coalesce(payload->>'category', '')) not in ('Phone', 'Tablet', 'Laptop', 'Watch', 'Game Console', 'Other') then
    raise exception 'Device category is required';
  end if;
  if trim(coalesce(payload->>'brand', '')) = '' or trim(coalesce(payload->>'model', '')) = '' then
    raise exception 'Device brand and model are required';
  end if;
  if trim(coalesce(payload->>'condition_grade', '')) not in ('As New', 'Good', 'Fair', 'Poor', 'Faulty') then
    raise exception 'Device condition is required';
  end if;
  if trim(coalesce(payload->>'category', '')) = 'Phone' and normalized_imei_value = '' then raise exception 'IMEI is required for phones'; end if;
  if normalized_imei_value <> '' and length(normalized_imei_value) <> 15 then raise exception 'IMEI must contain 15 digits'; end if;
  if normalized_imei_value = '' and serial_value = '' then raise exception 'IMEI or serial number is required'; end if;
  if clean_status_value not in ('Clean', 'Blocked', 'Pending', 'Not Applicable') then raise exception 'Invalid device check status'; end if;
  if clean_status_value = 'Clean' and trim(coalesce(payload->>'clean_check_reference', '')) = '' then
    raise exception 'IMEI check reference is required for a clean result';
  end if;
  if status_value not in ('inspection', 'ready_for_sale') then raise exception 'New devices must start in inspection or ready for sale'; end if;
  if purchase_cost_value <= 0 or sale_price_value <= 0 then raise exception 'Purchase cost and sale price must be above zero'; end if;
  if trim(coalesce(payload->>'payout_method', '')) not in ('Cash', 'Bank Transfer') then raise exception 'Invalid payout method'; end if;

  if status_value = 'ready_for_sale' and (
    clean_status_value not in ('Clean', 'Not Applicable')
    or lower(coalesce(payload->>'activation_lock_removed', 'false')) <> 'true'
    or lower(coalesce(payload->>'data_erased_confirmed', 'false')) <> 'true'
    or (select count(*) from jsonb_each_text(inspection_value) result where lower(result.value) in ('pass', 'na')) < 12
    or exists (select 1 from jsonb_each_text(inspection_value) result where lower(result.value) not in ('pass', 'na'))
  ) then raise exception 'Ready-for-sale devices must pass compliance and inspection checks'; end if;

  insert into public.pos_used_device_acquisitions (
    acquisition_code, store_id, shift_id, seller_name, seller_phone, seller_email,
    seller_address, seller_id_type, seller_id_reference, seller_age_confirmed,
    seller_is_owner, owner_name, owner_address, acquisition_statement,
    ownership_declaration, payout_method, payout_amount, declaration_text,
    acquired_by
  ) values (
    acquisition_code_value,
    selected_store.id,
    nullif(trim(payload->>'shift_id'), ''),
    seller_name_value,
    seller_phone_value,
    lower(trim(coalesce(payload->>'seller_email', ''))),
    seller_address_value,
    trim(payload->>'seller_id_type'),
    trim(payload->>'seller_id_reference'),
    true,
    seller_is_owner_value,
    trim(coalesce(payload->>'owner_name', '')),
    trim(coalesce(payload->>'owner_address', '')),
    trim(payload->>'acquisition_statement'),
    true,
    trim(payload->>'payout_method'),
    purchase_cost_value,
    declaration_value,
    selected_staff.display_name
  ) returning * into acquisition_row;

  insert into public.pos_used_devices (
    device_code, acquisition_id, store_id, category, brand, model, variant,
    color, storage, imei, normalized_imei, serial_number, condition_grade,
    battery_health, inspection, clean_check_status, clean_check_reference,
    clean_checked_at, activation_lock_removed, data_erased_confirmed,
    purchase_cost, sale_price, status, notes, photo_urls, acquired_by,
    acquired_at, updated_by, ready_at
  ) values (
    device_code_value,
    acquisition_row.id,
    selected_store.id,
    trim(payload->>'category'),
    trim(payload->>'brand'),
    trim(payload->>'model'),
    trim(coalesce(payload->>'variant', '')),
    trim(coalesce(payload->>'color', '')),
    trim(coalesce(payload->>'storage', '')),
    trim(coalesce(payload->>'imei', '')),
    normalized_imei_value,
    serial_value,
    trim(payload->>'condition_grade'),
    nullif(payload->>'battery_health', '')::integer,
    inspection_value,
    clean_status_value,
    trim(coalesce(payload->>'clean_check_reference', '')),
    case when clean_status_value in ('Clean', 'Blocked') then now() else null end,
    lower(coalesce(payload->>'activation_lock_removed', 'false')) = 'true',
    lower(coalesce(payload->>'data_erased_confirmed', 'false')) = 'true',
    purchase_cost_value,
    sale_price_value,
    status_value,
    trim(coalesce(payload->>'notes', '')),
    case when jsonb_typeof(payload->'photo_urls') = 'array' then payload->'photo_urls' else '[]'::jsonb end,
    selected_staff.display_name,
    acquisition_row.acquired_at,
    selected_staff.display_name,
    case when status_value = 'ready_for_sale' then now() else null end
  ) returning * into device_row;

  insert into public.pos_used_device_transactions (
    transaction_code, device_id, store_id, transaction_type, from_status,
    to_status, amount, payment_method, staff_name, counterparty_name,
    counterparty_phone, notes, transaction_payload
  ) values (
    'UDTX-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 16)),
    device_row.id,
    selected_store.id,
    'acquisition',
    null,
    device_row.status,
    purchase_cost_value,
    acquisition_row.payout_method,
    selected_staff.display_name,
    acquisition_row.seller_name,
    acquisition_row.seller_phone,
    'Device purchased from seller',
    jsonb_build_object('acquisition_code', acquisition_row.acquisition_code)
  );

  return jsonb_build_object('ok', true, 'device', public.pos_used_device_payload(device_row));
end;
$$;

create or replace function public.update_pos_used_device(session_token text, payload jsonb)
returns jsonb
language plpgsql
security definer
set search_path = ''
as $$
declare
  selected_store public.store_locations%rowtype;
  selected_staff public.staff_directory%rowtype;
  device_row public.pos_used_devices%rowtype;
  previous_status text;
  previous_price numeric(12,2);
  status_value text;
  price_value numeric(12,2);
  clean_status_value text;
  clean_reference_value text;
  activation_lock_value boolean;
  data_erased_value boolean;
  inspection_value jsonb;
  event_type text;
begin
  if not public.is_valid_staff_session(session_token) then raise exception 'Invalid session'; end if;

  select * into selected_store
  from public.store_locations store_location
  where store_location.active = true
    and store_location.store_code = coalesce(trim(payload->>'store_code'), '')
    and store_location.store_code <> 'warehouse';
  if not found then raise exception 'Store not found'; end if;

  select * into selected_staff
  from public.staff_directory staff
  where staff.active = true and lower(staff.display_name) = lower(coalesce(trim(payload->>'staff_name'), ''))
  limit 1;
  if not found then raise exception 'Staff member not found'; end if;

  select * into device_row
  from public.pos_used_devices device
  where device.device_code = coalesce(trim(payload->>'device_code'), '')
    and device.store_id = selected_store.id
  for update;
  if not found then raise exception 'Used device not found'; end if;
  if device_row.status = 'sold' then raise exception 'Sold devices cannot be manually changed'; end if;
  if device_row.status in ('returned_to_seller', 'disposed') then raise exception 'Closed device records cannot be changed'; end if;

  previous_status := device_row.status;
  previous_price := device_row.sale_price;
  status_value := coalesce(nullif(trim(payload->>'status'), ''), device_row.status);
  price_value := round(coalesce(nullif(payload->>'sale_price', '')::numeric, device_row.sale_price), 2);
  clean_status_value := coalesce(nullif(trim(payload->>'clean_check_status'), ''), device_row.clean_check_status);
  clean_reference_value := coalesce(payload->>'clean_check_reference', device_row.clean_check_reference);
  activation_lock_value := case
    when payload ? 'activation_lock_removed' then lower(payload->>'activation_lock_removed') = 'true'
    else device_row.activation_lock_removed
  end;
  data_erased_value := case
    when payload ? 'data_erased_confirmed' then lower(payload->>'data_erased_confirmed') = 'true'
    else device_row.data_erased_confirmed
  end;
  inspection_value := case
    when jsonb_typeof(payload->'inspection') = 'object' then payload->'inspection'
    else device_row.inspection
  end;

  if status_value not in ('inspection', 'ready_for_sale', 'returned_to_seller', 'disposed') then raise exception 'Invalid device status'; end if;
  if price_value <= 0 then raise exception 'Sale price must be above zero'; end if;
  if clean_status_value not in ('Clean', 'Blocked', 'Pending', 'Not Applicable') then raise exception 'Invalid device check status'; end if;
  if clean_status_value = 'Clean' and trim(clean_reference_value) = '' then raise exception 'IMEI check reference is required for a clean result'; end if;
  if status_value = 'ready_for_sale' and (
    clean_status_value not in ('Clean', 'Not Applicable')
    or not activation_lock_value
    or not data_erased_value
    or (select count(*) from jsonb_each_text(inspection_value) result where lower(result.value) in ('pass', 'na')) < 12
    or exists (select 1 from jsonb_each_text(inspection_value) result where lower(result.value) not in ('pass', 'na'))
  ) then raise exception 'Device cannot be marked ready until all checks pass'; end if;

  update public.pos_used_devices
  set status = status_value,
      sale_price = price_value,
      clean_check_status = clean_status_value,
      clean_check_reference = clean_reference_value,
      clean_checked_at = case
        when clean_status_value in ('Clean', 'Blocked') and clean_status_value <> device_row.clean_check_status then now()
        else clean_checked_at
      end,
      activation_lock_removed = activation_lock_value,
      data_erased_confirmed = data_erased_value,
      inspection = inspection_value,
      notes = coalesce(payload->>'notes', notes),
      updated_by = selected_staff.display_name,
      ready_at = case when status_value = 'ready_for_sale' then coalesce(ready_at, now()) else null end
  where id = device_row.id
  returning * into device_row;

  if previous_price <> device_row.sale_price then
    insert into public.pos_used_device_transactions (
      transaction_code, device_id, store_id, transaction_type, from_status,
      to_status, amount, staff_name, notes, transaction_payload
    ) values (
      'UDTX-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 16)),
      device_row.id, device_row.store_id, 'price_change', previous_status,
      device_row.status, device_row.sale_price, selected_staff.display_name,
      coalesce(trim(payload->>'notes'), 'Sale price updated'),
      jsonb_build_object('previous_price', previous_price, 'sale_price', device_row.sale_price)
    );
  end if;

  if previous_status <> device_row.status then
    event_type := case device_row.status
      when 'returned_to_seller' then 'returned_to_seller'
      when 'disposed' then 'disposal'
      else 'status_change'
    end;
    insert into public.pos_used_device_transactions (
      transaction_code, device_id, store_id, transaction_type, from_status,
      to_status, amount, staff_name, notes
    ) values (
      'UDTX-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 16)),
      device_row.id, device_row.store_id, event_type, previous_status,
      device_row.status, 0, selected_staff.display_name,
      coalesce(trim(payload->>'notes'), 'Device status updated')
    );
  end if;

  return jsonb_build_object('ok', true, 'device', public.pos_used_device_payload(device_row));
end;
$$;

create or replace function public.prepare_pos_used_device_sale_line()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
declare
  device_row public.pos_used_devices%rowtype;
  order_row public.pos_sales_orders%rowtype;
  device_code_value text;
begin
  if lower(coalesce(new.line_payload->>'is_used_device', 'false')) <> 'true' then return new; end if;

  device_code_value := coalesce(
    nullif(trim(new.line_payload->>'used_device_id'), ''),
    nullif(regexp_replace(new.product_id, '^used-', ''), new.product_id)
  );
  if device_code_value is null then raise exception 'Used device id is required'; end if;

  select * into order_row from public.pos_sales_orders where id = new.sales_order_id;
  select * into device_row
  from public.pos_used_devices device
  where device.device_code = device_code_value
  for update;
  if not found then raise exception 'Used device not found'; end if;
  if device_row.store_id <> order_row.store_id then raise exception 'Used device belongs to another store'; end if;
  if device_row.status <> 'ready_for_sale' then raise exception 'Used device is no longer available for sale'; end if;
  if new.quantity <> 1 then raise exception 'Used devices can only be sold once per line'; end if;
  if round(new.unit_price, 2) <> round(device_row.sale_price, 2) then raise exception 'Used device sale price has changed'; end if;

  new.line_type := 'used_device';
  new.used_device_id := device_row.id;
  new.product_id := 'used-' || device_row.device_code;
  new.sku := device_row.device_code;
  return new;
end;
$$;

create or replace function public.complete_pos_used_device_sale_line()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
declare
  device_row public.pos_used_devices%rowtype;
  order_row public.pos_sales_orders%rowtype;
begin
  if new.used_device_id is null then return new; end if;
  select * into order_row from public.pos_sales_orders where id = new.sales_order_id;

  update public.pos_used_devices
  set status = 'sold',
      sold_order_id = order_row.id,
      sold_order_line_id = new.id,
      sold_at = order_row.created_at,
      updated_by = order_row.staff_name
  where id = new.used_device_id
  returning * into device_row;

  insert into public.pos_used_device_transactions (
    transaction_code, device_id, store_id, transaction_type, from_status,
    to_status, amount, payment_method, related_sales_order_id,
    related_sales_order_line_id, staff_name, counterparty_name,
    counterparty_phone, notes, transaction_payload
  ) values (
    'UDTX-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 16)),
    device_row.id, device_row.store_id, 'sale', 'ready_for_sale', 'sold',
    new.line_total, order_row.payment_method, order_row.id, new.id,
    order_row.staff_name, order_row.customer_name, order_row.customer_phone,
    'Used device sold on invoice #' || order_row.invoice_number,
    jsonb_build_object('buyer_address', coalesce(new.line_payload->>'buyer_address', ''))
  );
  return new;
end;
$$;

drop trigger if exists pos_sales_order_lines_prepare_used_device on public.pos_sales_order_lines;
create trigger pos_sales_order_lines_prepare_used_device
before insert on public.pos_sales_order_lines
for each row execute function public.prepare_pos_used_device_sale_line();

drop trigger if exists pos_sales_order_lines_complete_used_device on public.pos_sales_order_lines;
create trigger pos_sales_order_lines_complete_used_device
after insert on public.pos_sales_order_lines
for each row execute function public.complete_pos_used_device_sale_line();

create or replace function public.return_refunded_pos_used_device()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
declare
  sales_line public.pos_sales_order_lines%rowtype;
  refund_row public.pos_sales_refunds%rowtype;
  device_row public.pos_used_devices%rowtype;
  refunded_total numeric(12,2);
begin
  select * into sales_line from public.pos_sales_order_lines where id = new.sales_order_line_id;
  if sales_line.used_device_id is null then return new; end if;

  select round(coalesce(sum(amount), 0), 2) into refunded_total
  from public.pos_sales_refund_lines where sales_order_line_id = sales_line.id;
  if refunded_total < sales_line.line_total then return new; end if;

  select * into refund_row from public.pos_sales_refunds where id = new.refund_id;
  update public.pos_used_devices
  set status = 'inspection',
      sold_order_id = null,
      sold_order_line_id = null,
      sold_at = null,
      ready_at = null,
      updated_by = refund_row.staff_name
  where id = sales_line.used_device_id
    and status = 'sold'
    and sold_order_line_id = sales_line.id
  returning * into device_row;

  if found then
    insert into public.pos_used_device_transactions (
      transaction_code, device_id, store_id, transaction_type, from_status,
      to_status, amount, payment_method, related_sales_order_id,
      related_sales_order_line_id, staff_name, counterparty_name,
      counterparty_phone, notes, transaction_payload
    ) values (
      'UDTX-' || upper(substr(replace(gen_random_uuid()::text, '-', ''), 1, 16)),
      device_row.id, device_row.store_id, 'refund_return', 'sold', 'inspection',
      refunded_total, refund_row.method, sales_line.sales_order_id, sales_line.id,
      refund_row.staff_name, '', '', 'Fully refunded device returned for inspection',
      jsonb_build_object('refund_code', refund_row.refund_code, 'reason', refund_row.reason)
    );
  end if;
  return new;
end;
$$;

drop trigger if exists pos_sales_refund_lines_return_used_device on public.pos_sales_refund_lines;
create trigger pos_sales_refund_lines_return_used_device
after insert on public.pos_sales_refund_lines
for each row execute function public.return_refunded_pos_used_device();

create or replace function public.get_pos_shift_payment_totals(session_token text, target_shift_code text)
returns jsonb
language plpgsql
security definer
set search_path = ''
as $$
declare
  shift_row public.pos_store_shifts%rowtype;
  totals_payload jsonb;
begin
  if not public.is_valid_staff_session(session_token) then raise exception 'Invalid session'; end if;

  select * into shift_row
  from public.pos_store_shifts shift_record
  where shift_record.shift_code = coalesce(trim(target_shift_code), '')
  limit 1;
  if not found then raise exception 'Shift not found'; end if;

  with received as (
    select payment.method, round(sum(payment.amount), 2) as amount
    from public.pos_sales_order_payments payment
    join public.pos_sales_orders sales_order on sales_order.id = payment.sales_order_id
    where sales_order.store_id = shift_row.store_id
      and sales_order.shift_id = shift_row.shift_code
    group by payment.method
  ),
  refunded as (
    select refund.method, round(sum(refund.amount), 2) as amount
    from public.pos_sales_refunds refund
    where refund.store_id = shift_row.store_id
      and refund.created_at >= shift_row.opened_at
      and (shift_row.closed_at is null or refund.created_at <= shift_row.closed_at)
    group by refund.method
  ),
  paid_out as (
    select acquisition.payout_method as method, round(sum(acquisition.payout_amount), 2) as amount
    from public.pos_used_device_acquisitions acquisition
    where acquisition.store_id = shift_row.store_id
      and acquisition.shift_id = shift_row.shift_code
    group by acquisition.payout_method
  ),
  methods as (
    select method from received
    union select method from refunded
    union select method from paid_out
  )
  select coalesce(jsonb_agg(jsonb_build_object(
    'method', methods.method,
    'received', coalesce(received.amount, 0),
    'refunded', coalesce(refunded.amount, 0),
    'paid_out', coalesce(paid_out.amount, 0),
    'amount', coalesce(received.amount, 0) - coalesce(refunded.amount, 0) - coalesce(paid_out.amount, 0)
  ) order by methods.method), '[]'::jsonb)
  into totals_payload
  from methods
  left join received on received.method = methods.method
  left join refunded on refunded.method = methods.method
  left join paid_out on paid_out.method = methods.method;

  return jsonb_build_object(
    'ok', true,
    'shift_id', shift_row.shift_code,
    'payment_totals', totals_payload,
    'order_count', (
      select count(*) from public.pos_sales_orders sales_order
      where sales_order.store_id = shift_row.store_id and sales_order.shift_id = shift_row.shift_code
    ),
    'gross_sales', (
      select coalesce(sum(sales_order.total), 0) from public.pos_sales_orders sales_order
      where sales_order.store_id = shift_row.store_id and sales_order.shift_id = shift_row.shift_code
    ),
    'used_device_acquisition_count', (
      select count(*) from public.pos_used_device_acquisitions acquisition
      where acquisition.store_id = shift_row.store_id and acquisition.shift_id = shift_row.shift_code
    ),
    'used_device_acquisition_spend', (
      select coalesce(sum(acquisition.payout_amount), 0) from public.pos_used_device_acquisitions acquisition
      where acquisition.store_id = shift_row.store_id and acquisition.shift_id = shift_row.shift_code
    )
  );
end;
$$;

revoke execute on function public.pos_used_device_payload(public.pos_used_devices) from public, anon, authenticated;
revoke execute on function public.validate_pos_used_device_acquisition_shift() from public, anon, authenticated;
revoke execute on function public.search_pos_used_devices(text, text, text, text, integer) from public, anon, authenticated;
revoke execute on function public.get_pos_used_device_transactions(text, text, text, integer) from public, anon, authenticated;
revoke execute on function public.create_pos_used_device_acquisition(text, jsonb) from public, anon, authenticated;
revoke execute on function public.update_pos_used_device(text, jsonb) from public, anon, authenticated;
revoke execute on function public.prepare_pos_used_device_sale_line() from public, anon, authenticated;
revoke execute on function public.complete_pos_used_device_sale_line() from public, anon, authenticated;
revoke execute on function public.return_refunded_pos_used_device() from public, anon, authenticated;

grant execute on function public.search_pos_used_devices(text, text, text, text, integer) to anon, authenticated, service_role;
grant execute on function public.get_pos_used_device_transactions(text, text, text, integer) to anon, authenticated, service_role;
grant execute on function public.create_pos_used_device_acquisition(text, jsonb) to anon, authenticated, service_role;
grant execute on function public.update_pos_used_device(text, jsonb) to anon, authenticated, service_role;
